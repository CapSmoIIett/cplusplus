
можно рассматривать шаблон функции как имеющий следующий вид: 
```c++
	template<typename Т> void f(ParamType param) ;
```

Тип, выведенный для т, зависит не только от типа expr, но и от вида Pa ramType. Существует три случая:

1. ParamType представляет собой указатель или ссылку, но не универсальную ссылку.
2. ParamType является универсальной ссылкой.
3. ParamType не является ни указателем, ни ссылкой.

### 1. ParamType представляет собой указатель или ссылку, но не универсальную ссылку

В этом случае вывод типа работает следующим образом.
1. Если типом expr является ссылка, ссылочная часть игнорируется. 
2. Затем выполняется сопоставление типа expr с ParamType для определения T.


Например, если у нас имеются шаблон 
```c++
template<typename Т> void f (T& param) ; // param представляет собой ссылку

int х = 27 ; // х имеет тип int 
const int сх = х ; // с х имеет тип const int 
const int& rx = х; // rx является ссыпкой на х как на const int

//выводимые типы для param и Т в различных выводах будут следующими: 
f(х); // Т - int , тип param - int& 
f(cx); // Т - const int , тип param - const int& 
f(rx); // Т - const int , тип param - const int&
```

Пример 2:

```c++
template<typename Т> 
void f (const Т& param) ; // param является ссылкой на const 

int х = 27 ; 
const int сх = х ; 
const int& rx = х; 

f(х); // T - int ,тип param - const int& 
f(сх); // т int , тип param - const int& 
f(rx); // т int , тип pa ram - const int&
```

Пример 3:
```c++
template<typename Т> 
void f (const Т* param) ; // param является указателем 

int х = 27 ; 
const int* px = &х; // рх - указатель на х, как на const int

f(&x); // Т - iпt , тип param - int*
f(рх); // Т - const int, тип param - const int*
```

### 2. ParamType является универсальной ссылкой

1. Если expr представляет собой lvalue, как Т, так и ParamType выводятся как lvаluе ссылки. Это вдвойне необычно. Во-первых, это единственная ситуация в выводе типа шаблона, когда Т выводится как ссылка. Во-вторых, хотя ParamType объявлен с использованием синтаксиса rvаluе-ссылки, его выводимым типом является lvаluе-ссылка.
2. Если expr представляет собой rvalue, применяются "обычные" правила (из случая l).

Примеры:
```c++
template<typename Т> 
void f (Т&& param) ; // param является универсальной ссылкой 

int х = 27 ; 
const int сх = х ; 
const int& rx = х; 

f(х); // х - lvalue, так что Т - int & , 
	//тип pararn также является int& 
f(сх); // сх - lvalue , так что Т - const int& , 
	//тип pararn также является const int& 
f(rx); // rx - lvalue , так что Т - const int& , 
	// тип pararn также является const int& 
f(27); // 27 - rvalue , так что Т - int , 
	//следовательно, тип param - int & &
```

### 3. ParamType не является ни указателем, ни ссылкой

Когда Pa rarnType не является ни указателем, ни ссылкой, мы имеем дело с передачей по значению.

Это означает, что pararn будет копией переданного функции - совершенно новым объектом. Тот факт, что pararn будет совершенно новым объектом, приводит к правилам, которые регулируют вывод Т из expr.

1. Как и ранее, если типом expr является ссылка, ссылочная часть игнорируется.
2. Если после отбрасывания ссылочной части expr является const, это также игнорируется. Игнорируется и модификатор volatile.

Пример:
```c++
template<typename Т> 
void f (Т param) ; // param является указателем 

int х = 27 ; 
const int сх = х ; 
const int& rx = х; 

f(х); // Типами и Т , и param являются int
f(сх); // Типами и Т , и param вновь являются iпt
f(rx); // T int , тип param - const int&

const char* const ptr = // ptr - константный указатель на 
	"Fun with pointers" ; // константный объект

f(ptr); // T - типа const char* const, param - const char*
```


const справа от звездочки объявляет ptr константным: pt r не может ни указывать на другое место в памяти, ни быть обнуленным.
const слева от звездочки гласит, что ptr указывает на то, что (строка символов) является const, а следовательно, не может быть изменено.

В соответствии с правилом вывода типа при передаче параметров по значению константность pt r будет проигнорирована, а выведенным для param типом будет const char*, т.е. изменяемый указатель на константную строку символов.

### Аргументы массивы

```c++
template<typename Т> 
void f (Т param) ; // param является указателем

сonst char name [ ] = "Briggs" ; // Тип name - const char [1З]

f(name); // nаmе - массив, но Т - const char* 


template<typename Т> 
void f (const Т& param) ; // param является ссылкой на const 

f(name); // T - const char [13], param - const char (&) [13]
```

возможность объявлять ссылки на массивы позволяет создать шаблон, который выводит количество элементов, содержащихся в массиве:

```c++
template<typeпame Т, std::size_t N> 
constexpr std::size_t arraySize (T(&)[N]) поехсерt
{
	return N;
}
```

### Аргументы функции

```c++
void someFunc ( int, double ); // someFunc - функция; 
// ее тип - void ( iп t , douЬle) 
	
template<typeпame Т> 
void f1 (Т param); // В f1 param передается по значению

template<typeпame Т> 
void f2 (T& param); // В f2 param передается по ссылке

f1 (someFunc}; // param выводится как указатель на функцию
	// тип - void ( * ) ( int , double)
f2 (someFunc); // param выводится как ссыпка на функцию; 
	// тип - void ( & ) ( int, double )
```
### Если кратко

В процессе вывода типа шаблона аргументы, являющиеся ссылками, рассматриваются как ссылками не являющиеся, т.е. их "ссылочность" игнорируется.

При выводе типов для параметров, являющихся универсальными ссылками, lvalue apryмeнты рассматриваются специальным образом.

При выводе типов для параметров, передаваемых по значению, аргументы, объяв ленные как const и/или volatile, рассматриваются как не являющиеся ни const, ни volatile.

 В процессе вывода типа шаблона аргументы, являющиеся именами массивов или функций, преобразуются в указатели, если только они не использованы для инициализации ссылок.